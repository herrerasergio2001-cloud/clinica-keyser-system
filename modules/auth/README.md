# Auth Module

NestJS implementation lives in `apps/api/src/modules/auth`.

Responsibilities:

- Login and logout
- JWT access tokens
- Refresh token rotation
- Session control
- Audit logging for auth events
